#need to create ns first or figure out a way to do it in helm

cmd to install
helm install weather-app .

run the ansible script to install metrics server first as it is required by the horizontal auto-scaler