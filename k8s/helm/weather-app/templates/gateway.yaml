apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: weather-gateway
  namespace: weather-app
spec:
  gatewayClassName: cilium

  listeners:
  - name: http
    protocol: HTTP
    port: 80
    hostname: weather.mattmootz.com
